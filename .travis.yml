language: node_js

node_js:
  - "7.9.0"

services:
  - mongodb

before_install:
  - npm i -g npm@4.2.0

install:
  - npm install

script:
  - ./node_modules/.bin/eslint .
  - npm test

notifications:
  email: false
  slack:
    on_success: change
    on_failure: always
    rooms:
      - hipsterello:8dxZ3Xs9P1HYX9MSrsFCLeRO
